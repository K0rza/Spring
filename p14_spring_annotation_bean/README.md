# Project Description

In this project, the usage of @Bean is illustrated.

The SwimCoach class is not described as @Component, thus the class is not a Spring Bean. However, to include the project in to the Spring framework, @Bean annotation is used in factory method.

# Preliminary
@Bean is used to import a 3rd party object in to Spring Framework.

The factory class is notted with @Configuration.